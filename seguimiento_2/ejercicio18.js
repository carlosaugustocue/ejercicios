//Ejercicio 18
let a = 4;
let b = true;
a++;        // Incrementa a, a se convierte en 5.
a += 3;     // Suma 3 a a, a se convierte en 8.
a %= 3;     // a módulo 3, a se convierte en 2.
let z = (a > 3 && b) ? 4 : 3;  // Evalúa si a > 3 y b es verdadero.
console.log(z);//Resultado: 3
